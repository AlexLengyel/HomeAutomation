/*
doc = document.getElementById("living-room");
doc.style.fillOpacity = 0.5 + 0.1;
console.log(doc.style.fillOpacity);

const controlPanel = document.getElementById("control-panel");

const div = document.createElement("div");
div.innerText = "Div";

const paragraph = document.createElement("p");
paragraph.innerText = "P";

div.appendChild(paragraph);
controlPanel.appendChild(div);

const checkbox = document.getElementById("living-room/overhead-light");
checkbox.checked = true;
console.log(checkbox.checked);
*/

/*
const cb = () => console.log("cb");

const path = "../data/data2.json"

var readJson = (path, cb) => {
    fs.readFile(require.resolve(path), (err, data) => {
      if (err)
        cb(err)
      else
        cb(null, JSON.parse(data))
    })
  }*/
